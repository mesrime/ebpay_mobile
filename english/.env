# ==========================================================
# Exemple de fichier de configuration .env
# ----------------------------------------------------------
# Ce fichier contient toutes les variables d’environnement
# nécessaires à la connexion sécurisée à ta base PostgreSQL.
#
# ⚠️ À NE PAS PARTAGER PUBLIQUEMENT (contient des identifiants sensibles)
#
# Pour l’utiliser :
#   1️⃣ Copie ce fichier et renomme-le simplement en ".env"
#   2️⃣ Remplis-le avec tes propres informations de connexion
#   3️⃣ Le fichier "config.py" chargera automatiquement ces valeurs
# ==========================================================


# ----------------------------------------------------------
# Adresse du serveur PostgreSQL
# ----------------------------------------------------------
# Généralement "localhost" si la base est en local.
# Peut aussi être une adresse IP (ex: 192.168.1.10) ou un domaine
PG_HOST=localhost


# ----------------------------------------------------------
# Port PostgreSQL
# ----------------------------------------------------------
# Le port par défaut est 5432, ne change que si ton serveur
# PostgreSQL écoute sur un autre port.
PG_PORT=5432


# ----------------------------------------------------------
# Nom de la base de données à utiliser
# ----------------------------------------------------------
# C’est la base que ton application va utiliser pour lire/écrire des données.
PG_DBNAME=ebpay


# ----------------------------------------------------------
# Nom d’utilisateur PostgreSQL
# ----------------------------------------------------------
# L’utilisateur qui a les droits d’accès à la base définie ci-dessus.
PG_USER=postgres


# ----------------------------------------------------------
# Mot de passe de cet utilisateur
# ----------------------------------------------------------
# ⚠️ Garde cette information confidentielle.
# C’est ce qui protège ta base contre un accès non autorisé.
PG_PASSWORD=santos75


# ----------------------------------------------------------
# Mode SSL (sécurisation des connexions)
# ----------------------------------------------------------
# - disable → aucune sécurité SSL (recommandé pour développement local)
# - prefer  → SSL si disponible, sinon connexion normale
# - require → connexion chiffrée obligatoire (utile pour production/cloud)
PG_SSLMODE=prefer


# ----------------------------------------------------------
# Taille minimale du pool de connexions
# ----------------------------------------------------------
# Nombre minimal de connexions simultanées ouvertes en permanence.
# 1 est suffisant pour les petits projets ou le développement.
PG_POOL_MIN=1


# ----------------------------------------------------------
# Taille maximale du pool de connexions
# ----------------------------------------------------------
# Nombre maximal de connexions ouvertes en même temps.
# Ajuste cette valeur selon la charge prévue sur ton application.
PG_POOL_MAX=10
